# WorkoutStartModal - Visual Flow Diagrams

## 1. Quest Completion Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    QUEST PAGE (Quest.jsx)                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
                    ┌─────────────────────┐
                    │ User clicks "START  │
                    │ QUEST" button       │
                    └─────────────────────┘
                              │
                              ↓
        ┌─────────────────────────────────────────┐
        │  handleOpenStartModal()                 │
        │  setShowStartModal(true)                │
        └─────────────────────────────────────────┘
                              │
                              ↓
        ┌──────────────────────────────────────────────────┐
        │         WorkoutStartModal Opens                  │
        │  ┌──────────────────────────────────────────┐   │
        │  │ Time Input: ___ (e.g., "30")             │   │
        │  │ [START]              [CLOSE]             │   │
        │  └──────────────────────────────────────────┘   │
        └──────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    ↓                   ↓
            User clicks        User clicks
            "START"             "CLOSE"
            │                   │
            ↓                   ↓
    ┌──────────────────┐  handleClose()
    │ Timer Starts     │  → Modal closes
    │ 30:00 → 29:59    │  → No session
    │ → 00:00          │     created
    └──────────────────┘
            │
            ↓ Timer finishes
    ┌──────────────────────┐
    │ handleModalComplete()│
    └──────────────────────┘
            │
            ↓
    ┌──────────────────────────────────┐
    │ API POST /quests/complete/{id}   │
    │ ✓ Quest marked complete          │
    │ ✓ User gets XP                   │
    │ ✓ Redirects to home              │
    └──────────────────────────────────┘
```

---

## 2. Workout Logging Flow

```
┌────────────────────────────────────────────────────────────────┐
│              WORKOUT PAGE (Workout.jsx)                        │
└────────────────────────────────────────────────────────────────┘
                         │
                         ↓
    ┌──────────────────────────────────────┐
    │  Form:                               │
    │  [Workout Name: _______________]    │
    │  [Sets: __] [Reps: __]              │
    │  [START WORKOUT] [LOG WORKOUT]      │
    └──────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        ↓                                 ↓
   User clicks                      User clicks
   "START WORKOUT"                  "LOG WORKOUT"
   │                                │
   ↓                                ↓ (Skip timer)
   Modal Opens                      API POST /workouts
   Time Input                        ✓ Workout logged
   │
   ↓
   User enters "30"
   │
   ↓
   ┌────────────────────┐
   │ Timer Starts:      │
   │ 30:00 → 00:00      │
   │ Keep app open!     │
   └────────────────────┘
   │
   ├─────────────────────────────┐
   ↓                             ↓
Timer Finishes              User Cancels
   │                             │
   ↓                             ↓
handleModalComplete()     handleModalCancel()
   │                             │
   ↓                             ↓
API POST /workouts         ⚠ Not logged
✓ Workout logged           ✓ Session invalidated
✓ Form cleared             ✓ User notified
✓ Added to list
```

---

## 3. localStorage & Session Persistence

```
┌─────────────────────────────────────────────────────┐
│           NORMAL COMPLETION FLOW                    │
└─────────────────────────────────────────────────────┘

Timer Starts:
┌───────────────────────────────────────┐
│ localStorage["activeWorkoutSession"]: │
│ {                                     │
│   id: "quest-123-1700000000000",      │
│   type: "quest",                      │
│   startTime: 1700000000000,           │
│   estimatedDuration: 1800000,         │
│   isActive: true                      │
│ }                                     │
└───────────────────────────────────────┘
                 ↓ every 1 second
        ┌────────────────────┐
        │ estimatedDuration  │
        │ decreases by 1000  │
        └────────────────────┘
                 ↓
         Timer completes
                 ↓
┌───────────────────────────────────────┐
│ localStorage updated:                 │
│ {                                     │
│   ...same data...                     │
│   isActive: false,                    │
│   endTime: 1700000001800000           │
│ }                                     │
└───────────────────────────────────────┘
                 ↓
        localStorage cleared
                 ↓
         onComplete() called


┌─────────────────────────────────────────────────────┐
│         PAGE RELOAD DETECTION FLOW                  │
└─────────────────────────────────────────────────────┘

User starts timer:
┌────────────────┐
│ isActive:true  │
│ localStorage ✓ │
└────────────────┘
        │
        ├─ User refreshes page
        │ (F5 / Ctrl+R)
        │
        ↓ Page reloads
┌──────────────────────────┐
│ useEffect() on mount:    │
│ checkForUnfinishedSesson()
└──────────────────────────┘
        │
        ↓
┌──────────────────────────────────┐
│ Read localStorage                │
│ Found: isActive = true           │
│ (means user didn't finish!)      │
└──────────────────────────────────┘
        │
        ↓ Automatically invalidate
┌──────────────────────────────────┐
│ Update in localStorage:          │
│ isActive: false                  │
│ endTime: [NOW]                   │
└──────────────────────────────────┘
        │
        ↓
┌──────────────────────────────────┐
│ Call onCancel() callback         │
│ Parent notifies user:            │
│ "Unfinished workout invalidated" │
└──────────────────────────────────┘
        │
        ↓
    Clear localStorage


┌─────────────────────────────────────────────────────┐
│     CLOSE APP BEFORE COMPLETION FLOW                │
└─────────────────────────────────────────────────────┘

Timer running:
┌─────────────────┐
│ isActive: true  │
└─────────────────┘
        │
        ├─ User closes browser tab/app
        │
        ↓ Page unloads (no chance to cleanup)
┌─────────────────────────────────────┐
│ localStorage still has:             │
│ {                                   │
│   id: "workout-123...",             │
│   isActive: true,  ← STILL TRUE     │
│   ...                               │
│ }                                   │
└─────────────────────────────────────┘
        │
        ├─ User opens browser again
        ├─ Navigates back to site
        │
        ↓ Page loads
┌──────────────────────────────────┐
│ checkForUnfinishedSession():     │
│ Detects isActive: true           │
│ Knows user closed app mid-timer  │
└──────────────────────────────────┘
        │
        ↓ Invalidate immediately
┌──────────────────────────────────┐
│ Mark as completed but not valid: │
│ isActive: false                  │
│ endTime: [NOW]                   │
│ → onCancel() called              │
│ → localStorage cleared           │
└──────────────────────────────────┘
```

---

## 4. Component State Transitions

```
┌──────────────────────────────────────────────────────────────┐
│           WorkoutStartModal State Machine                    │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐
│  CLOSED STATE   │
│ isStarted=false │
│ timeInput="30"  │
│ remainingMs=0   │
└─────────────────┘
        │
        │ isOpen=true
        ├─ User sees:
        │ • Time input field
        │ • [START] [CLOSE] buttons
        │ • No timer
        │
        ├──────────────────┬──────────────────┐
        ↓                  ↓                  ↓
   Invalid Input      User clicks        User clicks
   (e.g., "-5")      "CLOSE"             "START"
        │                │                   │
        ↓                ↓                   ↓
   Show error        handleClose()      handleStart()
   Stay in state     → Exit modal       → Parse time
                     → onClose()        → If valid:
                                        │
                                        ↓
                        ┌─────────────────────────────┐
                        │    TIMER STATE              │
                        │ isStarted=true              │
                        │ remainingMs=1800000         │
                        │ timerRef=active             │
                        └─────────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ↓ every 1000ms          ↓ User action
                    │                   handleCancel()
            remainingMs -= 1000            │
                    │                       │
             ┌──────┴──────┐                ├─ Clear timer
             │             │                ├─ Invalidate session
        Countdown       remainingMs         ├─ onCancel()
        animation       reaches 0           └─ Reset state
             │             │
             │             ↓
             │       handleTimerComplete()
             │             │
             └─────────────┘
                     │
                     ↓
        ┌─────────────────────────────┐
        │ UPDATE LOCALSTORAGE:        │
        │ isActive: false             │
        │ endTime: [NOW]              │
        └─────────────────────────────┘
                     │
                     ↓
        ┌─────────────────────────────┐
        │ CALL PARENT HANDLER:        │
        │ onComplete(sessionData)     │
        └─────────────────────────────┘
                     │
                     ↓
        ┌─────────────────────────────┐
        │ RESET MODAL STATE:          │
        │ isStarted: false            │
        │ timeInput: "30"             │
        │ remainingMs: 0              │
        │ error: ""                   │
        └─────────────────────────────┘
                     │
                     ↓
        ┌─────────────────────────────┐
        │ MODAL CLOSES                │
        │ (parent should call         │
        │  onClose() or              │
        │  setShowModal(false))       │
        └─────────────────────────────┘
```

---

## 5. Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────┐
│  USER INPUT → VALIDATION → STORAGE → TIMER → CALLBACK         │
└────────────────────────────────────────────────────────────────┘

     ┌──────────────────┐
     │  User enters     │
     │  time in input   │
     │  "1:30:45"       │
     └──────────────────┘
            │
            ↓
     ┌──────────────────┐
     │ parseTimeInput() │
     │ "1:30:45"        │
     │ → regex split    │
     │ → validate parts │
     └──────────────────┘
            │
            ├─ Valid? → 5445000 (ms)
            │
            └─ Invalid? → return null
                         → show error
                         → user retries

    (Valid case continues)
            │
            ↓
    ┌──────────────────────┐
    │ saveSessionToStorage()│
    │                      │
    │ localStorage =       │
    │ {                    │
    │  id: unique-id       │
    │  startTime: now()    │
    │  estimatedDuration   │
    │  isActive: true      │
    │ }                    │
    └──────────────────────┘
            │
            ↓
    ┌──────────────────────┐
    │ startCountdown()     │
    │ setInterval(1000ms)  │
    │                      │
    │ Each iteration:      │
    │ • timeLeft -= 1000   │
    │ • update ui          │
    │ • update localStorage│
    │ • check if done      │
    └──────────────────────┘
            │
    ┌───────┴───────┐
    │               │
    ↓               ↓
 Timer           Timer
continues       reaches 0
    │               │
    │               ↓
    │        handleTimerComplete()
    │               │
    │               ├─ Clear interval
    │               ├─ Update localStorage
    │               ├─ onComplete(session)
    │               └─ resetModal()
    │
    └─→ (loop continues)
```

---

## 6. Integration Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    Quest.jsx / Workout.jsx                   │
│                        (Parent)                              │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ State:                                              │   │
│  │ • showStartModal: boolean                           │   │
│  │ • questInProgress: boolean                          │   │
│  │ • quest/workoutData: object                         │   │
│  │                                                     │   │
│  │ Handlers:                                           │   │
│  │ • handleOpenStartModal()                            │   │
│  │ • handleModalComplete(sessionData)                  │   │
│  │ • handleModalCancel(sessionData)                    │   │
│  │ • handleComplete() / handleSubmit()                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                         │                                    │
│                         │ passes props:                      │
│                         │ isOpen                             │
│                         │ onClose                            │
│                         │ onComplete                         │
│                         │ onCancel                           │
│                         │ workoutId                          │
│                         │ type                               │
│                         ↓                                    │
│         ┌─────────────────────────────────┐                │
│         │ WorkoutStartModal (Child)       │                │
│         │                                 │                │
│         │ Internal State:                 │                │
│         │ • timeInput: string             │                │
│         │ • isStarted: boolean            │                │
│         │ • remainingMs: number           │                │
│         │ • error: string                 │                │
│         │                                 │                │
│         │ localStorage Management:        │                │
│         │ • saveSessionToStorage()        │                │
│         │ • clearSessionFromStorage()     │                │
│         │ • checkForUnfinishedSession()   │                │
│         │                                 │                │
│         │ UI:                             │                │
│         │ • Time input or countdown       │                │
│         │ • START / CANCEL buttons        │                │
│         │ • Progress bar                  │                │
│         └─────────────────────────────────┘                │
│                         │                                    │
│                         │ calls parent callbacks:            │
│                         │ onComplete(sessionData)            │
│                         │ onCancel(sessionData)              │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Parent handler executes:                            │   │
│  │ • completeQuest() / logWorkout()                    │   │
│  │ • API POST to backend                              │   │
│  │ • Update user profile/XP                           │   │
│  │ • Show success message                             │   │
│  │ • Reset form state                                 │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. Time Format Parsing Examples

```
INPUT: "30"
  ↓
Split by ":"? No (no colon)
  ↓
Parse as integer: 30
  ↓
Convert to milliseconds: 30 × 60 × 1000 = 1,800,000ms
  ↓
Result: 30 minutes ✓

INPUT: "1:30"
  ↓
Split by ":": ["1", "30"]
  ↓
Parse: minutes=1, seconds=30
  ↓
Convert: (1×60 + 30) × 1000 = 90,000ms
  ↓
Result: 1 min 30 sec ✓

INPUT: "1:30:45"
  ↓
Split by ":": ["1", "30", "45"]
  ↓
Parse: hours=1, minutes=30, seconds=45
  ↓
Validate: hours ≥ 0 ✓, minutes < 60 ✓, seconds < 60 ✓
  ↓
Convert: (1×3600 + 30×60 + 45) × 1000 = 5,445,000ms
  ↓
Result: 1h 30m 45s ✓

INPUT: "abc"
  ↓
Split by ":"? No
  ↓
Parse as integer: NaN ✗
  ↓
Return null
  ↓
Show error: "Invalid time format" ✗
```

---

## 8. Error Handling Flow

```
┌──────────────────────────────────┐
│   User Input or Action           │
└──────────────────────────────────┘
        │
        ├─────────────────────────┐
        ↓                         ↓
   Time Input           Any Error Event
        │                         │
        ├─ Parse error            ├─ localStorage error
        │                         ├─ clearInterval error
        │                         ├─ JSON.parse error
        ↓                         │
   ┌────────────────┐            ↓
   │ Show error msg │     ┌─────────────────┐
   │ "Invalid time  │     │ console.error() │
   │ format"        │     │ Graceful degrade│
   │ Try again      │     │ Continue anyway │
   └────────────────┘     └─────────────────┘
        │
        ↓
   ┌─────────────────┐
   │ Error cleared   │
   │ when user       │
   │ modifies input  │
   └─────────────────┘
```

---

These diagrams show how all the pieces fit together! 🎯
